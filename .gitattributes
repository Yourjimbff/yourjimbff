# The version bump is the one line every parallel branch touches. It lives in its
# own file now, and merges resolve to the higher version instead of stopping.
# The driver is registered per clone — see CLAUDE.md. Without it you just get a
# normal one-line conflict.
VERSION merge=maxversion
