# Generated by the offline test harness: a copy of index.html with SB_URL rewritten to
# a dead port so every failure path can be exercised without touching the real project.
# Regenerate it, never commit it — it carries a broken SB_URL and would ship that.
.serve-test.html
.feedcorpus.js
.sundaycorpus.js
.likescorpus.js
.barcheck.html
.macrow.html
.pfrow.html
.debrief.html
.debrief.txt

# account snapshots for the baseline habit — never committed
.food-baseline/

# A real Client File is a coaching record — red lines, personal details.
# It is never committed. The whole repo has been publicly served (see
# netlify.toml); a pending ingestion lives outside the repo until it is saved.
scripts/tests/fixtures-ben-*.txt
scripts/tests/fixtures-*-real.txt
