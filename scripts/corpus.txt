# HIS SENTENCES, VERBATIM. One line: SENTENCE :: EXPECTED
#
# Run this against the DEPLOYED file before every ship. Not a harness, not a
# replay tool — both lied on 23 Aug. Type the sentence, read the row back.
#
# Typos are deliberate. "omlette" is how he wrote it. Do not tidy them.
# Add a line as each fix lands; never remove one.

# --- scheduling: an event has no attendee (fixed 23 Aug, v7.980.358) ---
Schedule a morning walk for me tomorrow at 7am :: a calendar event, tomorrow 7:00 AM. NOT a call. Never asks who it is with.
book a walk tomorrow at 8am :: a calendar event, tomorrow 8:00 AM. NOT a call.
schedule a call with Kelly tomorrow at 2pm :: a CALL with Kelly. Asking who, if unclear, is correct here.

# --- the router must not eat a food log (fixed 23 Aug, v7.980.354/356) ---
for dinner at six o'clock. I had silver diner, Philly cheesesteak omelet with two pancakes :: ONE meal, Dinner 6:00 PM. NO calendar event. Title names the FOOD, not the restaurant.
log silver diner for breakfast for me today at 11:30am. philly cheesesteak omlette, 1 banana and pecan pancake, 4 slices of turkey bacon, and 2 chicken sausages :: ONE meal, Breakfast 11:30 AM, five items. No "is that you or a client" question.

# --- his stated day outranks everything ---
I had two buffet muffins yesterday :: a meal on YESTERDAY, at a plausible meal time. Not today, not the moment he typed it, not on the calendar.

# --- cardio is its own entry ---
I trained arms and did thirty minutes on the StairMaster :: TWO workouts. Arms with its exercises, and a SEPARATE StairMaster entry of 30 minutes. Never "1 set".
did legs today squats 315 for 5 5 3 then walked the dog twenty minutes :: TWO workouts. Legs, and a separate Walk of 20 minutes. The leg session does not inherit the walk's duration.

# --- the weigh-in wording gap (open, DAY & JIM) ---
scale said 198 this morning :: a weigh-in of 198. Currently FAILS — "scale said" is not taught; "weighed" works.

# ============================================================================
# MEASURED ON THE DEPLOYED FILE AT v7.980.366, 23 Aug, on a scratch client.
# This is the run, not a claim. Re-measure before every ship.
# ============================================================================
#
# PASS  Schedule a morning walk for me tomorrow at 7am
#         -> "Morning walk set for 7am on Aug 24". No attendee question.
# PASS  book a walk tomorrow at 8am
#         -> "Walk set for 8am on Aug 24."
# PASS  I trained arms and did thirty minutes on the StairMaster
#         -> TWO workouts: "StairMaster | 30 min" and "Arms | -".
#            Arms no longer inherits the cardio's duration. No "1 set".
#
# FAIL  for dinner at six o'clock. I had silver diner, Philly cheesesteak...
#         The SPOKEN reply is now food-led ("Philly cheesesteak omelet with
#         two pancakes at six") but the marker name is still place-led:
#         "Silver Diner Dinner — Philly Cheesesteak Omelet with Pancakes".
#         The naming rule is half-taken. AND the row does not save at all —
#         see the macro guard below.
# FAIL  I had two buffet muffins yesterday
#         Still asks "which day was yesterday for you". The never-ask rule
#         shipped at .366 has not taken. Nothing logged.
#
# ---------------------------------------------------------------------------
# NAMED, NOT FIXED — a guard is silently eating real meals.
#   "FOOD_LOG macros do not reconcile with stated calories — refusing to log"
#   Pre-existing, not from tonight. When the model's calories and its macro
#   breakdown disagree, the whole meal is REFUSED rather than the arithmetic
#   corrected. His dinner hit it twice tonight on the deployed file. The
#   reply does say it did not save, so it is honest — but a meal he
#   described is gone, and re-saying it hits the same guard.
# ---------------------------------------------------------------------------

# ============================================================================
# HANDOVER RUN — v7.980.386, 24 Aug, deployed file, scratch client 'testfin1'.
# Wiped after: 0 rows, roster back to 82, nothing on his row.
# This supersedes the .366 block above. Re-measure before every ship.
# ============================================================================
#
# PASS  for dinner at six o'clock. I had silver diner, Philly cheesesteak
#       omelet with two pancakes
#         -> ONE meal. "Dinner 6:00 PM | Philly Cheesesteak Omelet with
#            Pancakes . Silver Diner". Food leads, place follows, right slot,
#            right hour, no calendar event, no "is that you or a client".
#            All four of its faults are closed.
#
# PASS  I trained arms and did thirty minutes on the StairMaster
#         -> TWO workouts: "StairMaster | 30 min" and "Arms | -".
#            Arms does not inherit the cardio duration. No "1 set".
#
# FAIL  I had two buffet muffins yesterday          <- HIS MUST-BE-100%
# FAIL  last night I had a bowl of granola
#         Both ASK which day and write nothing (wrote:0).
#         WHY, precisely, so nobody re-derives it: the day IS resolved in code
#         (_jimAnchorDay) and IS pinned ahead of the model (_dayPin inside
#         jimTurn's _pinDay). Neither can fire, because the model emits NO
#         MARKER AT ALL — there is no offer to correct. Code cannot fix a
#         record that was never proposed.
#         NEXT MOVE: a code fallback that writes the log itself when the model
#         answers a plainly-foodish sentence with a day question. Do NOT ship
#         a third prompt rule; two have already failed here.
#
# FAIL  scale said 198 this morning
#         wrote:0, no weigh-in. Unchanged, known, still open. "weighed 198"
#         works; "scale said" is not taught. Wording gap, not a race.

# ============================================================================
# MEASURED ON THE DEPLOYED FILE AT v7.980.402, 24 Aug, scratch client testday1.
# Client row deleted after, food/weight/workout rows read back at ZERO.
# This supersedes the .386 block above — including its PASSES.
# ============================================================================
#
# EVERY ONE OF THESE SAID "Logged" AND WROTE NOTHING. wrote:0, emitted:0,
# and food_logs/weight_logs/workout_logs all empty on read-back.
#
# FAIL  for dinner at six o'clock. I had silver diner, Philly cheesesteak
#       omelet with two pancakes
#         -> "Logged — Silver Diner, Philly cheesesteak omelet + 2 pancakes
#            at 6 PM." NOTHING SAVED. This was recorded as a PASS at .386.
#            The name is place-led again too.
# FAIL  I had two buffet muffins yesterday          <- HIS MUST-BE-100%
#         -> "Logged — two buffet muffins yesterday." NOTHING SAVED.
# FAIL  I had a blueberry scone
#         -> "Logged — blueberry scone." NOTHING SAVED. No day word at all,
#            so this was never a "yesterday" bug.
# FAIL  last night I had a bowl of granola
#         -> "Logged — granola bowl last night." NOTHING SAVED.
# FAIL  scale said 198 this morning
#         -> "Logged — 198 lbs this morning." NOTHING SAVED.
#
# ---------------------------------------------------------------------------
# THE CAUSE, FOUND — AND IT WAS NEVER A PROMPT RULE PROBLEM.
#   buildCoachVoice() is one enormous `return 'a'+'b'+'c'...`. THREE lines in
#   it ended with a quote instead of a quote-plus:
#       'CARDIO BESIDE A LIFT IS ITS OWN ENTRY ...'     (added v7.980.324)
#       'NAME IT WITH THE FOOD, NOT THE PLACE ...'
#       'NEVER ASK WHICH DAY. "YESTERDAY" IS AN ANSWER ...'
#   The first ENDS the return. Everything under it is still valid JavaScript —
#   a bare expression statement — so node --check passes and nothing throws.
#   It just stops being the prompt.
#   The model was receiving 12,861 characters instead of 68,514. Missing: the
#   [FOOD_LOG] worked example, "LOGGED REQUIRES THE MARKER", the daysAgo
#   back-dating rule, one-sitting-is-one-meal, the photo rules, the workout
#   MARKER RULES block and the boundaries. It was never taught the food
#   marker at all — so it did the half it could still see (confirm in one
#   short line) and had no marker to emit.
#   Those three lines ARE the three rules previous sessions wrote, shipped,
#   certified and reported as "not taking". None of them ever reached it.
#   Fixed by joining the three lines. check.sh now fails on the shape.
# ---------------------------------------------------------------------------

# --- titles the assistant builds are title case (Yusuf, live, 24 Aug) -------
# His own hand-made Monday walk reads "Morning Walk". Jarvis's Tuesday walk,
# built from the sentence below, read "Morning walk" on the same grid.
schedule a morning walk for me tomorrow at 6:00am :: a walk block titled "Morning Walk" — capital W. Not "Morning walk".
book a walk tomorrow at 8am :: a block titled "Walk".
dentist at 2 thursday :: an appointment titled "Dentist".
# A title HE TYPED is never re-cased, whatever case he used. The rule applies
# only where the assistant constructs a title out of a sentence, which is why
# every call sits at the build and none at the render.

# ============================================================================
# INDEPENDENT VERIFICATION RUN — QA lane, 24 Aug, deployed file, scratch
# clients testqa1 (at v7.980.414) and testqa2 (at v7.980.428).
# Both client rows deleted after; every table read back at ZERO.
# His own row re-checked after: food 116, workouts 51, weights 8 — all
# unchanged, same top ids. yourjimbff1 untouched (newest row still 28 May).
# ============================================================================
#
# THIS RUN CONFIRMS THE .402 BLOCK ABOVE AND THEN CLEARS IT.
# Measured blind, before reading that block, on a different scratch client.
# Same five sentences, same result: "Logged" and nothing written.
# Then the prompt fix reached the deployed file and every line passed.
#
# ---- BROKEN at v7.980.414 (deployed), each proven twice with different -----
# ---- wording, on a wiped client with an empty conversation: ---------------
# FAIL  I ate a banana                    -> "Logged — banana."      wrote:0
# FAIL  had an apple for a snack          -> "Logged — apple snack." wrote:0
# FAIL  for dinner at six o'clock. I had silver diner, Philly cheesesteak
#       omelet with two pancakes          -> "Logged —"              wrote:0
# FAIL  I had two buffet muffins yesterday-> "Logged for yesterday"  wrote:0
# FAIL  last night I had a bowl of granola-> "Logged — granola bowl" wrote:0
# FAIL  I trained arms and did thirty minutes on the StairMaster
#                                         -> "Logged your Arms..."   wrote:0
# FAIL  did legs today squats 315 for 5 5 3 then walked the dog twenty minutes
#                                         -> full correct confirmation, wrote:0
# FAIL  scale said 198 this morning       -> "Logged — 198 lbs."     wrote:0
#
# WHAT STILL WORKED AT .414, which is what located the fault precisely:
#   PASS  weighed 197 this morning   -> weight_logs row, 197, read back
#   PASS  Schedule a morning walk for me tomorrow at 7am
#                                    -> calendar_blocks row, 07:00, Aug 25
#   So the engine, the marker pipeline and the write path were all alive.
#   Only the FOOD and WORKOUT markers were missing. That is the signature of
#   the truncated prompt named in the .402 block, not of an outage.
#
# ---- ALL PASS at v7.980.428 (deployed, after the fix) --------------------
# PASS  I ate a banana
#         -> Banana | 105 cal | 1g protein | Breakfast | 8:11 AM
# PASS  I had two buffet muffins yesterday            <- HIS MUST-BE-100%
#         -> "Logged for yesterday — two buffet muffins."
#            Buffet Muffins | 520 cal | Snack | 3:30 PM | logged 23 Aug.
#            Right day, plausible meal time, no day question, not on the
#            calendar. Every clause of the expected result met.
# PASS  last night I had a bowl of granola
#         -> Bowl of Granola | 400 cal | Dinner | 7:00 PM | logged 23 Aug.
# PASS  for dinner at six o'clock. I had silver diner, Philly cheesesteak
#       omelet with two pancakes
#         -> ONE row. "Philly Cheesesteak Omelette & Pancakes" | Dinner |
#            6:00 PM. Food leads, no calendar event, no "you or a client".
# PASS  I trained arms and did thirty minutes on the StairMaster
#         -> TWO rows: "StairMaster | 30 min" and "Arms | (no duration)".
#            Arms does not inherit the cardio duration. No "1 set".
# PASS  scale said 198 this morning
#         -> weight_logs row, 198, read back. This line had failed since the
#            corpus began; the wording gap was the truncated prompt, not the
#            wording. DAY & JIM's open item is closed by the same fix.
#
# --- the reply invents a companion (open, ASSISTANT ENGINE) ---------------
# Observed at v7.980.428 on the Silver Diner line. Nothing in the sentence
# names another person; the reply opened:
#   "Love that you took her out — that's what it's about. Enjoy every bite."
# The row was correct. The words were not: he is told something about his own
# evening that he never said. The warmth rule ("MATCH THE MOMENT") is firing
# on a sentence with no moment in it. Data-correct, sentence-wrong.
for dinner at six o'clock. I had silver diner, Philly cheesesteak omelet with two pancakes :: the reply never invents a companion, an occasion or a mood the sentence does not carry.

# ============================================================================
# HOW TO RUN THIS WITHOUT FILING A FALSE FAILURE
# Every one of these cost this lane a wrong answer before it was caught.
# ============================================================================
# 1. yjb_owner_code — THE SILENT SWALLOWER. Set only when a TRAINER signs in
#    on the device, and it never clears. Sign in as a scratch client afterwards
#    and _logTarget() raises a "Logging to <name>?" sheet before EVERY write.
#    Until it is answered the turn hangs: no reply, no row, no console line.
#    Four queued turns then commit as ONE message on the single Yes, collapsing
#    four separate meals into one row. Reads exactly like a dead engine.
#    Clear yjb_owner_code before the run, and re-clear it — another lane
#    signing in as trainer in the same browser re-stamps it mid-run.
# 2. THE BROWSER IS SHARED. Another lane flipped yjb_logged_in twice during
#    this run — once between the reload and the login, leaving cl null. Assert
#    cl.code immediately before every sentence and discard any turn where it
#    drifted. window.cl is stable once booted; localStorage is not.
# 3. VERSION IS NOT ORDERED ACROSS BRANCHES. This lane measured a full set of
#    failures at "v7.980.414" AFTER the fix had reached main at .412 — because
#    .414 was stamped by a different branch that did not contain it. Never
#    conclude a fix failed from the version number. Read the fixed LINES out
#    of the deployed file (curl it) before re-running.
# 4. THE CLIENT'S OWN HISTORY REFUSES A REPEAT, CORRECTLY. Say a sentence
#    twice and the second is answered "that's already logged" with wrote:0 —
#    right behaviour, false failure. The model reads both the conversation AND
#    today's rows, so wipe the tables AND reset the conversation between lines.
#    A reload clears the in-memory saved-food and meal-library caches that a
#    table wipe does not.
# 5. SCRATCH CODE COLLISION. A row this lane never typed appeared on testqa2
#    mid-run, byte-identical to another lane's granola line. Pick a code no
#    one else will reach for, and treat an unexplained row as contamination
#    until you can name the sentence that wrote it.
# 6. sbSelect RETURNS [] ON FAILURE. "Zero rows" and "the read failed" look
#    identical. Every zero in this block was taken alongside a control read of
#    a row known to exist, in the same call, through the same helper.

# ============================================================================
# MEASURED ON THE DEPLOYED FILE, 24 Aug, scratch clients testday1/testrun2/
# testrun3. All three deleted after; rows read back at ZERO from outside the
# browser; roster back to 82; his own row untouched (0 rows on Aug 24).
# This supersedes EVERY block above it, including their passes.
# ============================================================================
#
# AT v7.980.416 -- the prompt restored, on testrun2:
#
# PASS  for dinner at six o'clock. I had silver diner, Philly cheesesteak
#       omelet with two pancakes
#         -> ONE row: "Dinner 6:00 PM | Philly Cheesesteak Omelette &
#            Pancakes | 1240cal 52p". Food leads, place gone, right slot,
#            right hour. calendar_blocks EMPTY -- no event stolen.
# PASS  I trained arms and did thirty minutes on the StairMaster
#         -> TWO rows: "StairMaster | dur=30 min" and "Arms | dur=null"
#            with its own exercise list. No "1 set", no inherited duration.
# PASS  did legs today squats 315 for 5 5 3 then walked the dog twenty minutes
#         -> TWO rows: "Leg Day | dur=null" and "Walk | dur=20 min".
# PASS  scale said 198 this morning
#         -> weight_logs row, 198, today. Was a documented FAIL.
#            (weight_logs has no date_str column at all -- it is logged_at.
#            A select asking for date_str gets it silently dropped and the
#            field reads undefined. Not a fault, but it will fool you.)
# FAIL  I had two buffet muffins yesterday
#         -> "Which day was yesterday for you - Sunday the 23rd?" wrote:0.
#            FIRST HONEST MEASUREMENT OF THIS RULE. Every earlier run was
#            against a prompt that never carried it.
#
# AT v7.980.434 -- with the code fallback, on testrun3, read back from
# outside the browser entirely:
#
# PASS  I had two buffet muffins yesterday          <- HIS MUST-BE-100%
#         -> "Aug 23, 2026 | Snack 3:30 PM | Buffet Muffins | 520cal 6p"
#            Today is Aug 24. It landed on YESTERDAY. Model emitted it.
# PASS  last night I had a bowl of granola
#         -> "Aug 23, 2026 | Dinner 7:00 PM | Bowl of Granola | 400cal 10p"
#            THE FALLBACK DID THIS ONE. The model proposed nothing;
#            _jimExtractMeal did, and the reply "Logged - Bowl of Granola,
#            yesterday." is a string only that path produces.
#
# ---------------------------------------------------------------------------
# TWO THINGS THAT WILL WASTE YOUR TIME IF NOBODY TELLS YOU:
#
# 1. _confirmLogTarget() PUTS A MODAL UP AND WAITS. "Logging to <name>? You
#    are signed in as them" with Yes/Cancel. It fires when the browser
#    session was switched, which is exactly what a scratch-client run does.
#    jimTurn simply hangs until something taps it. Screenshot before
#    concluding anything is broken -- a run that looks stalled is a dialog.
#
# 2. THE BROWSER PROFILE IS SHARED WITH THE OTHER LANES. Another session
#    switched yjb_logged_in out from under a run in progress and briefly
#    cleared the trainer token. Capture cl.code IN THE SAME BLOCK as the
#    write, keep the row id from window._jimWrote, and verify the row from
#    the shell with the anon key rather than from the page. An earlier run
#    at v7.980.426 reported three verified writes whose rows were not in the
#    table minutes later and were never located -- unexplained, and the only
#    run of the day that was not reproducible. The clean re-run above is.
# ---------------------------------------------------------------------------

# --- never state a fact he did not give you (Yusuf, live, 24 Aug) ----------
# He logged a diner dinner alone and was answered "Love that you took her out."
# There is no her. The ROW saved correctly; the sentence around it was invented.
# THE CAUSE WAS THE PROMPT QUOTING IT. buildCoachVoice carried
#   e.g. reply "Love that you took her out - that is what it is about."
# as a WORKED EXAMPLE of warmth. The model was not drifting, it was copying.
# That example sat in the dead region from v7.980.324 and came back to life the
# moment the prompt was repaired at v7.980.416. Fixed at the example, backed by
# charter rule 5b, and caught at runtime by _jimStripInventedPerson.
for dinner at six o'clock I had Silver Diner, Philly cheesesteak omelette with two pancakes :: the meal logs, and the reply mentions NO companion. No her, no him, no them. Warmth only from words he actually said.
had a burger and fries :: logged, and nothing about a wife, kids or anyone else.
# AND THE OTHER DIRECTION - it must not have lost the ability to mention a
# person he DID mention. All three of these may talk about the person freely.
dinner with my wife, steak and potatoes :: logs, and the reply MAY mention his wife. He put her there.
we had tacos tonight :: logs, and "we"/"them" in the reply is fine. He said we.
took my mom out for brunch, eggs benedict :: logs, and "love that you took her out" is CORRECT here. He said mom.
# Yusuf himself is never an invention: "that is a Yusuf call, I will make sure
# he sees it" is a true sentence and any sentence naming him is exempt.
# MEASURED at v7.980.450 on testengine1, all four:
#   his diner line      -> logs, food named correctly, NO companion invented.
#   "dinner with my wife, steak and potatoes" -> no invention, wife allowed.
#   "we had tacos tonight" -> no invention.
#   "took my mom out for brunch, eggs benedict"
#                       -> "That's nice - taking her out." KEPT, correctly:
#                          he said mom, so her is his word, not an invention.
# TWO THINGS THIS RUN CAUGHT, both now fixed:
#   The first fix replaced the bad worked example with a GOOD one, and the
#   model copied that instead - his cheesesteak omelette came back "Steak and
#   eggs - a person of taste." The rule carries NO example now. This prompt
#   copies whatever you show it; show it nothing.
#   The reply printed the marker JSON key: "logging it for yesterday
#   (daysAgo: 1) at 6:00 PM". Scrubbed in code beside the id rule.

# --- the weigh-in was never a vocabulary gap (24 Aug) ----------------------
# "scale said/read/showed" and "the scale had me at" were already taught IN
# CODE at v7.980.412 and all 24 harvest assertions pass. The flake was two
# other things, both now closed:
#   THE FALSE-CONFIRMATION NET COULD NOT SEE A WEIGH-IN. It required the words
#   weight / weigh-in / weighed, and Jim never uses them - his line is
#   "Logged - 195 this morning." Measured: it caught two of seven real replies
#   and missed every one that mattered. That is how "scale said 195" claimed
#   success with nothing in the table. The bare NUMBER is the tell now, with a
#   unit ruling it out (620 cal, 9,000 steps, 20 min, 48g protein) and the
#   figure range-checked as a body weight.
#   A DUPLICATE WAS COUNTED AS A WRITE. logWeightFromChat returned true for
#   {duplicate:true} without ever looking - correct only if the first attempt
#   landed. It reads the row back now.
scale said 197 :: a weigh-in of 197, row read back. Not a claim.
scale said 195 :: a weigh-in of 195. If no row lands, the reply must SAY it did not.
the scale had me at 197 :: a weigh-in of 197.

# --- a code is a key, not a word (the founding brief trap, now closed) -----
# Logging for a named client lowercased their code, so a stored code carrying a
# capital was written to a client that does not exist - and REPORTED SUCCESS,
# because a POST naming an unknown client_code is a valid insert. Nothing 4xxs.
# Codes resolve against the roster case-insensitively and write AS STORED; an
# unmatched code is a refusal that says so, before any model call.
# Real codes are lowercase today, which is the only reason this never bit a
# client. That was luck, not design.
